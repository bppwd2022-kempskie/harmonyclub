<div class="container text-center">
  <p id="notice"><%= notice %></p>

  <p>
    <strong>Title:</strong>
    <%= @sponsor.title %>
  </p>

  <p>
    <strong>Website:</strong>
    <%= @sponsor.website %>
  </p>

  <%= link_to 'Edit', edit_sponsor_path(@sponsor), class:"btn btn-custom" %> |
  <%= link_to 'Back', sponsors_path, class:"btn btn-custom" %>
</div>
